export const THEME_PRIMARY = '#4E2A84';

export enum WORKS_CATEGORIES {
  largeEnsemble = 1,
  chamberSolo,
  filmVideo,
};

export enum WORKS_PATHS {
  largeEnsemble = 'large-ensemble',
  chamberSolo = 'chamber-solo',
  filmVideo = 'film-video',
}
